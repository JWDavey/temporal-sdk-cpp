// temporalio.cpp - Main library source (placeholder)
// This ensures the library target always has at least one compilation unit.

namespace temporalio {

// Library version information
const char* version() noexcept {
    return "0.1.0";
}

} // namespace temporalio
